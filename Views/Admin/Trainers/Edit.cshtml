@model GymMvc.ViewModels.TrainerEditViewModel

@{
    ViewData["Title"] = "Edit Trainer";
    ViewData["Dashboard"] = true;
}

@await Html.PartialAsync("_PageHeader", new Dictionary<string, string>
{
    ["Title"] = "Edit trainer",
    ["Description"] = "Update the trainer's profile, specialty, and certifications.",
    ["Eyebrow"] = "Operations / Trainers"
})

<div class="row g-4">

    <div class="col-lg-8">

        <section class="content-panel">

            <form asp-controller="AdminTrainers"
                  asp-action="Edit"
                  asp-route-id="@Model.Id"
                  method="post"
                  id="editTrainerForm">

                @Html.AntiForgeryToken()

                <input asp-for="Id" type="hidden" />

                <div asp-validation-summary="ModelOnly"
                     class="alert alert-danger"
                     role="alert"></div>

                <div class="row g-4">

                    <div class="col-md-6">

                        <label asp-for="FullName"
                               class="form-label">
                        </label>

                        <input asp-for="FullName"
                               class="form-control"
                               autocomplete="name"
                               placeholder="Trainer full name" />

                        <span asp-validation-for="FullName"
                              class="text-danger">
                        </span>

                    </div>

                    <div class="col-md-6">

                        <label asp-for="Specialty"
                               class="form-label">
                        </label>

                        <input asp-for="Specialty"
                               class="form-control"
                               placeholder="e.g. Strength & Conditioning" />

                        <span asp-validation-for="Specialty"
                              class="text-danger">
                        </span>

                    </div>

                    <div class="col-12">

                        <label asp-for="Certificates"
                               class="form-label">
                        </label>

                        <textarea asp-for="Certificates"
                                  class="form-control"
                                  rows="4"
                                  placeholder="List relevant certifications"></textarea>

                        <span asp-validation-for="Certificates"
                              class="text-danger">
                        </span>

                    </div>

                </div>

                <div class="d-flex justify-content-end
                            flex-wrap
                            gap-2
                            mt-4">

                    <a asp-controller="AdminTrainers"
                       asp-action="Index"
                       class="btn btn-iron-outline">
                        Cancel
                    </a>

                    <button type="submit"
                            class="btn btn-iron"
                            data-submit-loading>

                        <span data-submit-text>
                            Save changes
                        </span>

                        <span class="d-none"
                              data-submit-spinner>
                            <span class="spinner-border spinner-border-sm me-2"
                                  aria-hidden="true"></span>
                            Saving...
                        </span>

                    </button>

                </div>

            </form>

        </section>

    </div>

    <div class="col-lg-4">

        <section class="content-panel h-100">

            <p class="auth-kicker">
                TRAINER #@Model.Id
            </p>

            <h3 class="mb-3">
                Keep the profile current.
            </h3>

            <p class="text-secondary mb-0">
                Update the trainer's core information here.
                Classes, attendance, workout plans, and reviews
                remain managed through their respective systems.
            </p>

        </section>

    </div>

</div>

@section Scripts
{
    <partial name="_ValidationScriptsPartial" />

    <script>
        document.addEventListener("DOMContentLoaded", function () {

            const form =
                document.getElementById("editTrainerForm");

            if (!form) {
                return;
            }

            form.addEventListener("submit", function () {

                if (!form.checkValidity()) {
                    return;
                }

                const button =
                    form.querySelector("[data-submit-loading]");

                if (!button) {
                    return;
                }

                const text =
                    button.querySelector("[data-submit-text]");

                const spinner =
                    button.querySelector("[data-submit-spinner]");

                button.disabled = true;

                if (text) {
                    text.classList.add("d-none");
                }

                if (spinner) {
                    spinner.classList.remove("d-none");
                }
            });
        });
    </script>
}